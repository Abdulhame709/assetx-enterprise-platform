import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function ImportDataPage() {
  return (
    <PlaceholderPage
      title="Import Data"
      titleKey="nav.importData"
      subtitle="Asset and master-data import with reconciliation."
      subtitleKey="placeholder.importSubtitle"
    />
  );
}
