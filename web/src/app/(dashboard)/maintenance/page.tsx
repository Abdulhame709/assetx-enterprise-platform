'use client';

import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function MaintenancePage() {
  return <PlaceholderPage title="Maintenance" subtitle="Maintenance automation module — planned after backend capability (L5)." />;
}
