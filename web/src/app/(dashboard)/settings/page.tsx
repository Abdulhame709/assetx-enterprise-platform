import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      titleKey="nav.settings"
      subtitle="Tenant settings and preferences."
      subtitleKey="placeholder.settingsSubtitle"
    />
  );
}
