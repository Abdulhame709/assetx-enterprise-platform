'use client';

import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export default function InventoryPage() {
  return <PlaceholderPage title="Inventory" subtitle="Field inventory module — planned for Phase P3." />;
}
