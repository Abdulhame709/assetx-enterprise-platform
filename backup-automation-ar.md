# النسخ الاحتياطي اليومي إلى Google Drive

## الإعداد المعتمد

| البند | القيمة |
| --- | --- |
| الوجهة | Google Drive ضمن حساب Google Workspace المتصل |
| مجلد النسخ | `Energy Cost Intelligence System Backups` |
| وقت التشغيل | يوميًا، الساعة 01:00 بتوقيت اليمن (`Asia/Aden`) |
| الاحتفاظ | آخر 60 ملف ZIP غير موجود في سلة المهملات |
| سياسة الإزالة | نقل النسخ الأقدم إلى سلة المهملات فقط؛ لا حذف نهائي |
| مصدر الكود | فرع GitHub `manus-current-project` من `Abdulhame709/fuel-management-system` |

## ما يحدث في كل تشغيل

تستخرج المهمة المجدولة أحدث نسخة من فرع المشروع، ثم تشغّل `scripts/create-project-backup.mjs`. ينشئ ذلك أرشيفًا منظمًا ومنزوع الأسرار يضم المصدر، الإعدادات غير السرية، المخططات والترحيلات، ملفات الاعتماد، التوثيق، ودليل الاستعادة. يختبر التشغيل أرشيف ZIP ويعيد التحقق من `CHECKSUMS.sha256` قبل الرفع.

لا تتضمن النسخ أسرار البيئة، أو رموز الوصول، أو قاعدة البيانات السحابية الحية، أو ملفات البناء وملفات الاعتماد القابلة لإعادة الإنشاء. لاستعادة قاعدة البيانات أو الأصول السحابية، يلزم الاحتفاظ بتصدير مُدار مستقل من المنصة أو من مزود قاعدة البيانات.

## التحقق الأولي

رفعت نسخة اختبارية بنجاح إلى المجلد المحدد، وتحقق فحص الأرشيف من سلامته. كما نجحت 198 بصمة SHA-256 دون فشل. تمثل هذه النتيجة تحققًا من مسار الإنشاء والرفع؛ وسيؤكد أول تشغيل مجدول تلقائي استمرارية التنفيذ اليومي.

## الإدارة والاستعادة

يمكن لمالك المشروع متابعة حالة المهمة أو إيقافها أو استئنافها من لوحة إدارة الجداول الزمنية في المشروع. عند الحاجة إلى الاستعادة، نزّل ملف ZIP المطلوب من Google Drive، وافتح `RESTORE_GUIDE.md` الموجود داخله، ثم تحقق من البصمات باستخدام `CHECKSUMS.sha256` قبل تثبيت الاعتماديات وإعداد متغيرات البيئة وقاعدة البيانات.
