# بوابة جودة المنصة

تُطبّق هذه البوابة الحد الأدنى عالي الأثر من منهج الملف المرفق: اختبار سلوك المستخدم في المسارات، التحقق من الوصولية آليًا، وفحص الأصول المنشورة. تم اختيار Playwright مع محرك axe-core بدل إضافة حزمة أدوات كبيرة دفعة واحدة؛ فاختبارات Playwright تعتمد على محددات واجهة مرئية وعزل السياق، بينما يكتشف axe مشاكل وصولية آلية شائعة ولا يغني عن المراجعة البشرية.[1][2]

| المجال | التطبيق في المنصة | النتيجة الحالية |
|---|---|---|
| ثبات المسارات | 10 مسارات للطاقة والوقود مع رفض صفحات الخطأ والـ 404 | نجح محليًا وعلى الرابط المنشور |
| الوصولية | فحص WCAG A/AA للواجهة العامة عبر `@axe-core/playwright` | نجح بعد إزالة `maximum-scale=1` التي كانت تمنع التكبير على الجوال |
| تحميل الأصول | اختبار إنتاجي بمتصفح جديد مع عناوين أصول Vite المولدة | نجح؛ لا يعتمد الرابط على نسخة CSS قديمة |
| التحقق البصري | لقطات سطح المكتب والجوال لأهم صفحات الطاقة والوقود | الإطار والبطاقات والقائمة والحقول ثابتة في المعاينة المصادق عليها |
| الأمان الديناميكي | لم يُشغّل فحص هجومي على الإنتاج؛ يؤجل ZAP إلى بيئة تجريبية ونطاق مصرح به | مقصود لتجنب المساس بالبيانات التشغيلية |

## أوامر التحقق

| الأمر | الغرض |
|---|---|
| `pnpm test` | اختبارات منطق الخادم والواجهة الخفيفة |
| `pnpm test:e2e` | المسارات والوصولية في المعاينة أو عبر `E2E_BASE_URL` |
| `pnpm test:quality` | الأنواع والاختبارات والبناء والاختبارات الشاملة |

## الحدود المعروفة

الفحص الآلي لا يغطي الحكم البشري على سهولة القراءة أو كل معايير WCAG، لذلك تبقى مراجعة RTL والنصوص والألوان على أجهزة فعلية جزءًا من دورة الاعتماد. كما أن الصفحات الداخلية تحميها المصادقة؛ يفحص الاختبار الإنتاجي استقرار المسارات وحالة الدخول العامة، بينما تم فحص الحالات الداخلية بصريًا ضمن جلسة المعاينة.

## تحقق المسارات العميقة

أظهر الفحص المباشر للرابط المنشور `/energy/reports` مرحلتين سليمتين: هيكل تحميل ملون ومنظم أثناء الاستعلام عن الجلسة، ثم شاشة دخول موحّدة تخص قسم الطاقة والمولدات عند عدم وجود جلسة. لم تعد هذه المسارات تعرض الشاشة البيضاء الأحادية السابقة، كما أثبت اختبارا Playwright المساران العميقان للطاقة والوقود.

## تحقق الألوان والتخطيط

أظهرت المراجعة المرئية في جلسة المعاينة المصادق عليها لوحات `/energy` و`/energy/reports` و`/fuel` و`/fuel/inventory` على سطح المكتب والجوال. تستخدم البطاقات الآن أسطحًا بيضاء وظلالًا هادئة وشرائط تمييز ملونة، ولا تظهر خطوط سوداء فاصلة أو تجاوزات أفقية. كما تحقق الفحص الإنتاجي من أن حزمة `DashboardLayout` المنشورة تحتوي طبقة الألوان وشاشة الوصول الموحدة، واختبارات الإنتاج تثبت وصول المسارات والأصول الحديثة.

وأُجري تحقق قراءة فقط داخل جلسة مصادق عليها على الرابط المنشور `/energy`. أظهرت اللقطة الفعلية لوحة الطاقة بالبطاقات الملونة والحدود الناعمة والقائمة الجانبية المتسقة، من دون واجهة الدخول أو تجاوز أفقي. لا ينفذ هذا التحقق أي إنشاء أو تعديل أو حذف للبيانات.

ثم أُجري التحقق نفسه على رابط المعاينة المصادق عليه. تطابقت بنية الصفحة وعنوانها وبطاقات مؤشرات الطاقة وألوان الشرائط والقائمة الجانبية مع اللقطة المنشورة؛ والفرق الوحيد هو شريط المعاينة الخارجي الذي لا يظهر في الرابط العام ولا يتبع واجهة المنصة.

ولم يقتصر التحقق على المراجعة اليدوية: أُعيد إنشاء اللقطتين داخل جلستين مصادقتين للقراءة فقط، ثم قورنتا آليًا بعد استبعاد شريط المعاينة الخارجي. كانت نتيجة المقارنة على مساحة **1366 × 700** بمتوسط فرق لوني **0.0** ونسبة بكسلات مختلفة **0.0**؛ أي إن محتوى لوحة الطاقة في المعاينة والإنتاج متطابق بصريًا ضمن منطقة التطبيق.

## المراجع

[1]: https://playwright.dev/docs/best-practices "Playwright: Best Practices"
[2]: https://playwright.dev/docs/accessibility-testing "Playwright: Accessibility Testing"
