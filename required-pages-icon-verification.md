# Required pages icon verification

تم فتح صفحة `/locations` وصفحة `/asset-types` على رابط المعاينة العامة بعد تسجيل الدخول.

صفحة المواقع تعرض CommandToolbar موحداً بأوامر البحث والتحديث والطباعة/الحفظ كـ PDF والاستيراد وإنشاء موقع وإعادة ضبط البحث، دون أزرار نصية في الشريط. أزرار شجرة المواقع للإضافة والتعديل والحذف والتوسيع تعتمد رموزاً مع `title` و`aria-label`.

صفحة أنواع الأصول تعرض CommandToolbar موحداً بأوامر البحث والتحديث والطباعة/الحفظ كـ PDF والاستيراد وإنشاء النوع وإعادة ضبط البحث. أزرار الصفوف لإضافة نوع فرعي وإعادة التسمية والتعطيل تظهر كرموز مع tooltips، ولا تظهر أزرار نصية قديمة. نصوص الحفظ والإلغاء بقيت داخل نماذج الحوار فقط، وهي ليست أزرار شريط الصفحة.


صفحة `/inventory` تعرض التحديث والطباعة/الحفظ كـ PDF وإنشاء دورة كأيقونات فقط مع tooltips. فتح الدورة في كل بطاقة يستخدم رمز السهم مع `title` و`aria-label`، ولا توجد أزرار نصية قديمة.

صفحة `/movements` تعرض التحديث وتصدير CSV وتصدير PDF والطباعة/الحفظ كـ PDF وإزالة الفلاتر داخل CommandToolbar كأيقونات فقط مع tooltips. أزرار اعتماد الحركة ورفضها ومعاينتها داخل الجدول أيضاً أيقونات فقط مع `title` و`aria-label`. عناصر المرشحات والتبويبات بقيت نصية لأنها حقول اختيار وتبويبات بيانات وليست أزرار أوامر قديمة.
