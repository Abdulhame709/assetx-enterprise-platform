# تقييم بديل تصدير Excel

## القرار المبدئي

سيُستبدل الاعتماد الحالي على `xlsx` بمكتبة **`write-excel-file`** لتصدير التقارير فقط. يتوافق ذلك مع استخدام النظام الحالي، الذي ينشئ ملف XLSX من بيانات تقرير داخلية ولا يقرأ ملفات Excel يرفعها المستخدم.

| المعيار | `xlsx` الحالي | `write-excel-file` المقترح |
|---|---|---|
| نطاق الاستخدام في النظام | قراءة وكتابة عامة، رغم أن النظام يستخدم الكتابة فقط | كتابة ملفات XLSX في المتصفح أو Node.js |
| نشاط المشروع | إصدار npm الظاهر قديم | توجد تحديثات حديثة في المستودع العام |
| واجهة الاستخدام المطلوبة | بناء مصنف وورقة ثم تنزيل | تمرير صفوف البيانات وتوليد ملف تنزيل مباشرة |
| أسلوب الترحيل | — | تحويل صفوف التقرير الحالية إلى صفوف وخانات مكتوبة، مع اختبار الناتج |

لا يشكل هذا القرار ادعاءً بأن أي مكتبة خالية تمامًا من المخاطر. لكنه يقلل سطح الاعتماد إلى **كاتب XLSX فقط** يطابق حالة الاستخدام الفعلية، ويزيل مكتبة التصدير الحالية التي بقيت ضمن تنبيهات الاعتماديات.

## واجهة التنفيذ المعتمدة

توثق المكتبة مسار متصفح صريحًا `write-excel-file/browser`، وتولّد ملف التنزيل عبر `await writeExcelFile(sheetData, options).toFile('report.xlsx')`. كما تدعم عرضًا من اليمين إلى اليسار وتثبيت صف العناوين وتحديد أعمدة الملف؛ وهذه خصائص مناسبة لتقرير المنصة العربي. [2]

## مصادر التقييم

أظهر مستودع ExcelJS دعمه الواسع لدفاتر العمل، إلا أن واجهة المشروع أظهرت آخر إصدار منشور منذ عدة سنوات، لذلك لم يُختر للمسار الحالي. بالمقابل، يعرّف مستودع `write-excel-file` نفسه ككاتب XLSX للمتصفح وNode.js ويظهر نشاطًا أحدث في المستودع. [1] [2]

## حدود الترحيل

سيحتفظ الملف الجديد بالأعمدة الحالية: الدورة، الفترة، طاقة وتكلفة كل مصدر، تكلفة kWh، التوصية، الوفر، وسبب التوصية. ستظل ملفات PDF على مكتبتها الحالية لأن نطاق هذه الصيانة خاص بتصدير Excel.

## المراجع

[1]: https://github.com/exceljs/exceljs "ExcelJS على GitHub"
[2]: https://github.com/catamphetamine/write-excel-file "write-excel-file على GitHub"
