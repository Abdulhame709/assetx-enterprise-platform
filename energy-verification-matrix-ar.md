# مصفوفة تحقق قسم الطاقة

يوثق هذا الملف نطاق الاختبارات التي تحققت قبل اعتماد المنصة الموحدة. تعتمد الاختبارات على موجهات الخادم ومحركات الحساب، لذلك فهي تتحقق من فرض القواعد قبل أن تصل أي مدخلات إلى واجهة المستخدم.

| المحور | حالة التحقق | الاختبار أو الدليل |
|---|---|---|
| صلاحيات العمليات والفواتير | ناجح | `server/permissions.test.ts` و`server/routers/energy.expansion.test.ts` يتحققان من رفض الأدوار غير المصرح لها. |
| تعديل أو إضافة سجل في دورة مقفلة | ناجح | `server/routers/energy.closed-cycle.test.ts` يغطي قراءة العداد والفاتورة والتشغيل والوقود. |
| اعتماد وإقفال دورة الطاقة | ناجح | `server/routers/energy.status.test.ts` يغطي مرور الاعتماد والإقفال بهوية المراجع، ورفض الدور غير المصرح، ورفض الانتقال غير المسموح. |
| مقارنة التكلفة والتوصية | ناجح | `server/domain/costing.test.ts` يغطي توصية المصدر والتعادل ونقص البيانات. |
| تصدير Excel | ناجح | `server/routers/report-export.test.ts` يتحقق من صفوف `reportRowsForExport` التي تمرر إلى `XLSX.writeFile` وتشمل تكلفة الوحدة والتوصية والوفر. |
| تصدير PDF | ناجح | `server/routers/report-export.test.ts` يتحقق من نصوص `pdfReportLines` التي يكتبها مسار `jsPDF` لقيم المؤسسة والمولدات والتوصية. |
| رفع المرفقات | ناجح | `server/routers/attachments.test.ts` يغطي الامتداد غير المسموح، تجاوز 5 MB، وتوثيق منفذ الرفع من الجلسة. |
| عرض المرفقات | ناجح | `server/routers/attachments.test.ts` يتحقق من الرابط المؤقت وتسجيل هوية منفذ العرض. |

## حدود التحقق

تُنشأ ملفات Excel وPDF في المتصفح من بيانات التقرير بعد التحقق من محتوى مسارَي التصدير. يبقى تنزيل المتصفح الفعلي مرتبطًا بإعدادات جهاز المستخدم، لذلك يوصى بالنقر على زرّي التنزيل مرة واحدة عند الانتقال إلى بيئة النشر. لا يغير هذا القيد قواعد الحساب أو صلاحيات الخادم أو محتوى التقرير الذي تغطيه الاختبارات.
