# Preview Verification — 2026-08-14

تم تشغيل Backend المحلي على المنفذ `3001` بوضع `ASSETX_SEED_DEMO=1` مع استبعاد إعداد اتصال قاعدة البيانات الخارجية غير المتاح. أعاد مسار `GET /assets/analytics/summary` بيانات فعلية للعرض، منها 16 أصلاً موزعة على أربع فئات وخمسة مواقع.

استجاب رابط الواجهة العامة لصفحة الدخول، لكن محاولة الدخول التجريبي بقيت في الصفحة نفسها دون خطأ ظاهر في متصفح الاختبار. يلزم التحقق لاحقاً من متغير عنوان API المدمج في واجهة Next.js ومن مسار المصادقة بين النطاقين قبل اعتماد العرض المرئي النهائي.

تم التحقق من نجاح مسار المصادقة عبر بروكسي الواجهة (`/api/auth/login`) وأنه يعيد جلسة صالحة. عند إنشاء الجلسة التجريبية المتوافقة والانتقال إلى `/dashboard` ظلت الواجهة في حالة تحميل. هذا يشير إلى مشكلة واجهة أو استعادة جلسة تحتاج تشخيصاً مستقلاً، ولا يمنع اختبار API أو بيانات العرض الخلفية.

أعاد طلب بيانات لوحة المتابعة من داخل جلسة المتصفح استجابة `200` صالحة، كما تأكدت صلاحية الجلسة المخزنة ووجود المستأجر والصلاحيات. لذلك تتركز مشكلة المعاينة المرئية المتبقية في استعادة حالة واجهة React، لا في بيانات العرض أو بروكسي API أو صلاحية جلسة Backend.
