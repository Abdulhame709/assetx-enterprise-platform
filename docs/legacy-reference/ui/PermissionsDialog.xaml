<Window x:Class="AssetManagement.Views.PermissionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة الصلاحيات"
        Width="820" Height="560"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        Background="#F5F7FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="🔐 إدارة صلاحيات المستخدم"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#1565C0"/>
                <TextBlock x:Name="txtUserInfo"
                           FontSize="14"
                           Foreground="#616161"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgPermissions"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      FontSize="13"
                      BorderThickness="0"
                      RowHeight="38"
                      AlternationCount="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الصفحة / الوحدة"
                                        Binding="{Binding ModuleArabic}"
                                        Width="250"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="8,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridCheckBoxColumn Header="عرض" Binding="{Binding CanView}" Width="90"/>
                    <DataGridCheckBoxColumn Header="إضافة" Binding="{Binding CanAdd}" Width="90"/>
                    <DataGridCheckBoxColumn Header="تعديل" Binding="{Binding CanEdit}" Width="90"/>
                    <DataGridCheckBoxColumn Header="حذف" Binding="{Binding CanDelete}" Width="90"/>
                    <DataGridCheckBoxColumn Header="طباعة" Binding="{Binding CanPrint}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,15,0,0">
            <Button Content="✅ تحديد الكل"
                    Background="#43A047"
                    Foreground="White"
                    FontSize="13"
                    Padding="22,9"
                    Margin="5"
                    Cursor="Hand"
                    BorderThickness="0"
                    Click="BtnSelectAll_Click"/>

            <Button Content="⬜ إلغاء الكل"
                    Background="#FB8C00"
                    Foreground="White"
                    FontSize="13"
                    Padding="22,9"
                    Margin="5"
                    Cursor="Hand"
                    BorderThickness="0"
                    Click="BtnDeselectAll_Click"/>

            <Button Content="💾 حفظ"
                    Background="#1E88E5"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="Bold"
                    Padding="30,9"
                    Margin="5"
                    Cursor="Hand"
                    BorderThickness="0"
                    Click="BtnSave_Click"/>

            <Button Content="❌ إغلاق"
                    Background="#9E9E9E"
                    Foreground="White"
                    FontSize="13"
                    Padding="22,9"
                    Margin="5"
                    Cursor="Hand"
                    BorderThickness="0"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>