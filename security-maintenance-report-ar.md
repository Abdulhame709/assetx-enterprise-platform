# تقرير صيانة أمنية مختصر — تصدير Excel وأدوار الوقود

**تاريخ التحقق:** 22 أغسطس 2026  
**نطاق التغيير:** استبدال مسار تصدير Excel، فرض منح أدوار الوقود خادميًا، والتحقق من بناء الإنتاج والاعتماديات.

## ما تم إنجازه

تمت إزالة مكتبة `xlsx` من اعتماديات الإنتاج وترحيل التصدير إلى `write-excel-file` بإصدار 4.1.1. يستمر التقرير في إنشاء ملف Excel عربي باتجاه من اليمين إلى اليسار، مع رأس ثابت وأعمدة محددة، من دون إضافة أي وظيفة قراءة أو رفع ملفات Excel جديدة.

وأصبحت منح قسم الوقود مستقلة عن أدوار قسم الطاقة. يمر حساب **admin** فقط تلقائيًا؛ أما جميع الأدوار الأخرى، بما فيها `management` و`reviewer`، فتحتاج منحة وقود صريحة. تدعم الشاشة الإدارية منح أو سحب الأدوار `viewer` و`operator` و`supervisor` و`accountant` و`auditor`، بينما تستبدل العملية الخادمية المنح ذريًا وتضيف سجل تدقيق للقيم السابقة واللاحقة.

| بند التحقق | النتيجة |
|---|---|
| فحص TypeScript (`pnpm check`) | ناجح |
| اختبارات Vitest | 12 ملفًا و52 اختبارًا ناجحًا |
| اختبار ملف Excel | يتحقق من إنشاء ملف XLSX سليم يبدأ بتوقيع ZIP `PK` |
| بناء الإنتاج (`pnpm build`) | ناجح |
| اعتماد `xlsx` في شجرة الإنتاج | غير موجود |
| اختبار فصل أدوار الوقود | يثبت أن `management` يحتاج منحة `viewer` وأن `reviewer` بلا منحة يُرفض |
| معاينة شاشة المستخدمين | ناجحة داخل جلسة مدير على سطح المكتب والجوال؛ بطاقات الأدوار ومربعات الاختيار تُعرض دون تجاوز عرضي |

## التنبيهات المتبقية

أعاد تدقيق اعتماديات الإنتاج إظهار تنبيهات عالية ومتوسطة متعدية لا تتعلق بمسار تصدير Excel المستبدل. لم تُفرض لها تجاوزات إصدار داخل هذه الدورة، حتى لا يؤدي إصلاح ظاهري إلى كسر التخزين أو الخادم أو مكونات واجهة التقرير.

| الاعتماد المتبقي | المصدر في الشجرة | مستوى التدقيق | الإجراء المقترح |
|---|---|---|---|
| `fast-xml-parser` 5.2.5 | `@aws-sdk/client-s3` عبر `@aws-sdk/xml-builder` | عالٍ | اختبار تحديث متوافق لحزم AWS SDK في دورة مستقلة، مع التحقق من رفع وتنزيل المرفقات. |
| `path-to-regexp` 0.1.12 | `express` 4.21.2 | عالٍ | ترقية مدروسة لـ Express أو تحديث متوافق لسلسلة الاعتماديات، مع اختبار OAuth وtRPC. |
| `lodash` 4.17.21 | `recharts` 2.15.4 | عالٍ | متابعة إصدار متوافق من Recharts أو اعتماد محدث للحزمة دون تجاوز قسري. |
| `lodash-es` 4.17.21 | `streamdown` عبر Mermaid | عالٍ | مراجعة تحديث Streamdown/Mermaid وسلسلة محلل المخططات في دورة مخصصة. |
| `dompurify` وMermaid وحزمها | `streamdown` عبر Mermaid | متوسط/منخفض | حصر مدخلات المخططات غير الموثوقة ومتابعة الترقيات المتوافقة. |

> تمت معاينة العرض ضمن جلسة مدير فعلية على سطح المكتب والجوال. أما تغيير علامة اختيار وتشغيل عملية حفظ فعلية على مستخدم تشغيلي فلم يُنفذ خلال هذه الدورة كي لا تتغير صلاحيات مستخدم دون تكليف صريح؛ المسار الخادمي والواجهة مرتبطان ومغطيان بفحص الأنواع والاختبارات.
